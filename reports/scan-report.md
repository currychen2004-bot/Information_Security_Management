# 扫描与验证报告

## 验证对象

桌面宠物待办清单 Flask 应用：

`成员代码/桌面宠物代办清单-陈柯睿/app.py`

## 静态检查

命令：

```bash
python3 -m py_compile 成员代码/桌面宠物代办清单-陈柯睿/app.py
git diff --check
```

结果：

- Python 语法检查通过
- Git 空白与格式检查通过

## 接口安全验证

使用 Flask test client 进行接口级验证，覆盖认证、授权和待办主流程。

验证点：

- 未登录访问待办状态接口被拒绝
- 未登录访问账号概览接口被拒绝
- 注册用户成功
- 重复用户名不能注册
- 错误密码不能登录
- 正确密码可以建立会话
- 登录后可以访问账号概览接口
- 登录后可以新增、完成、删除任务
- 不同用户只能看到自己的待办数据
- 登出后不能继续访问待办数据
- 默认状态浅拷贝问题已修复，新用户不会共享同一个 `todos` 列表

结果：

全部通过。

## 结论

本次整改消除了待办数据接口默认公开访问的问题，补充了注册/登录模块、接口权限校验、用户数据隔离和登录后访问的账号概览接口，并保留了原有核心功能。
